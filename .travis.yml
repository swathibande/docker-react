sudo: required
services: 
  - docker

before install: 
  - sudo docker build -t swathibande/docker-react -f Dockerfile.dev .

script: 
  - sudo docker run swathibande/docker-react npm run test -- --coverage
    
